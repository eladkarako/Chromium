https://github.com/macchrome/chromium/releases/  

use NodeJS to parse the GitHub API JSON response as an object,  
from four latest releases in first-page in the release section,  
keep the latest Win64 (not macOS) that allows sync.  

information is written to stderr (so it can be ignored),  
url is written to stdout.  
exit code should be 0.  
